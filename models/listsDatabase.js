class ListsDatabase {
  constructor(listId, listName, movies, ownerId) {
    this.listId = listId;
    this.listName = listName;
    this.movies = movies;
    this.ownerId = ownerId;
  }
}

export default ListsDatabase;
